{
  "name": "monobank-mock",
  "version": "0.1.0",
  "private": true,
  "description": "Fixture-driven mock of the Monobank currency API with switchable failure modes",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24",
    "jest": "^30.5.1",
    "ts-jest": "^29.4.12"
  }
}
