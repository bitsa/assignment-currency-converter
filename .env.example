# Read by Docker Compose. Copy to .env to override.
APP_HOST_PORT=3000
REDIS_HOST_PORT=6390
LOG_LEVEL=info

# Application variables. Compose sets these for the app container with the values below;
# export them only when running the backend outside Compose.
PORT=3000
NODE_ENV=production
REDIS_URL=redis://redis:6379
